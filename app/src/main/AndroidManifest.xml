<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.guilhermeluftlab.befast">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/s_mbolo_24png"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BeFast">
        <activity android:name=".ServicosLogado"></activity>
        <activity android:name=".RegistroCompleto" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".UsuarioLogado"
            android:label="@string/title_activity_usuario_logado"
            android:theme="@style/Theme.BeFast.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>